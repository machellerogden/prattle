{
  "name": "alexa-prattle",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "git archive -o prattle.zip HEAD",
    "deploy": "aws s3 cp prattle.zip s3://mho-alexa-skills/",
    "ship": "npm run build && npm run deploy",
    "postinstall": "echo 'npm run ship' >| .git/hooks/post-commit && chmod +x .git/hooks/post-commit",
    "lint": "./node_modules/.bin/eslint .",
    "test": "./node_modules/.bin/lambda-local -l index.js -h handler -e ./test-events/my-car-is-ford.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^3.12.0",
    "lambda-local": "^1.3.0"
  }
}
